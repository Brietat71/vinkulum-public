[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "vinkulum-studio"
version = "0.2.0"
description = "Éditeur de mécanismes rigides 3D pour le noyau Vinkulum"
readme = "README.md"
requires-python = ">=3.14"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
dependencies = ["vinkulum==0.19.0", "PySide6==6.11.2", "vtk==9.7.0"]

[project.optional-dependencies]
test = ["scipy==1.18.1"]

[project.scripts]
vinkulum-studio = "vinkulum_studio.__main__:main"

[tool.setuptools.packages.find]
include = ["vinkulum_studio*"]
